name: Deploy to Production

on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Deploy (managed by Vercel)
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Deployment is handled automatically by Vercel on push to main.'
